# 双人共享桌宠 PRD

- 文档版本：v0.2（持续迭代）
- 状态：执行中；本文档是方向与验收基线，不限制基于真实体验做主动调整
- 目标平台：Windows 10/11 64 位
- 参考项目：[alvinunreal/openpets](https://github.com/alvinunreal/openpets)

## 1. 产品定义

这是一个供两位固定用户共同养育的 Windows 桌面宠物。两台电脑分别显示同一只宠物，双方的照顾、传话、礼物和成长进度通过一台云服务器同步。

产品的核心价值是轻松陪伴和两人之间的异步互动，而不是高压力的数值养成。最终版本同时支持异步和实时互动，第一版优先完成异步体验。

## 2. 已确认的产品决策

| 项目 | 决策 |
| --- | --- |
| 用户范围 | 两位固定用户私人使用 |
| 客户端 | 两台 Windows 电脑 |
| 服务端 | 一台云服务器 |
| 互动顺序 | 第一版异步，后续增加实时 |
| 养成强度 | 轻松陪伴型，无死亡、离家或永久损失 |
| 状态展示 | 混合模式：日常用表情和动作，面板显示模糊等级 |
| 角色 | 狸花纹与三花配色结合的猫 |
| 性格 | 活泼调皮 |
| 活动频率 | 默认适中，两台电脑可分别调整 |
| 原型素材 | 普通透明 PNG 加程序动画 |
| 正式素材 | 后续替换为 OpenPets 风格精灵图 |
| 成长奖励 | 解锁新表情与动作 |
| 技术策略 | 复用 OpenPets 桌宠底座，通过自定义插件接入独立云服务 |

## 3. 产品原则

1. 奖励双方参与，不惩罚暂时缺席。
2. 宠物的共享状态以云服务器为准，本机偏好只保存在各自电脑。
3. 断网时桌宠仍然可用，联网后自动补发操作。
4. 宠物传话不是完整聊天软件。
5. 活泼调皮不能以遮挡工作、频繁弹窗或持续发声为代价。
6. 第一版优先验证共同养成闭环，不实现商城、公开社交和复杂经济系统。

## 4. 用户与配对

### 4.1 私人配对

1. 云服务器创建一个宠物房间。
2. 服务器生成两个一次性邀请码。
3. 两台电脑分别使用一个邀请码绑定。
4. 绑定成功后向客户端发放可撤销的设备令牌。
5. 两位用户分别设置昵称。
6. 重装或更换电脑后可重新生成邀请码。

### 4.2 不在第一版范围内

- 公开注册。
- 密码找回。
- 用户搜索和好友系统。
- 陌生人访问。
- 社区、排行榜和公开宠物主页。

## 5. 共享数据与本机数据

### 5.1 云端共享数据

- 宠物名称和成长阶段。
- 心情、体力、饱食、共同亲密度。
- 两位用户的基础身份和各自熟悉度。
- 照顾事件、传话、礼物和解锁记录。
- 待发送、已接收和已展示的传话状态。

### 5.2 仅保存在本机

- 桌宠窗口位置。
- 宠物显示大小。
- 音量和静音。
- 活动频率。
- 是否置顶、开机启动和免打扰设置。
- 当前显示器及多显示器偏好。

## 6. MVP 功能需求

### 6.1 标准桌宠能力

- 透明、无边框、可置顶的桌宠窗口。
- 支持拖动、点击互动和鼠标穿透切换。
- 支持系统托盘、隐藏桌宠和退出程序。
- 支持开机启动。
- 支持多显示器和 Windows 缩放。
- 角色不能永久移动到屏幕可见区域之外。
- 可调整大小、音量和活动频率。
- 提供低打扰或免打扰模式。

### 6.2 共同照顾

| 操作 | 状态影响 | 角色反馈 |
| --- | --- | --- |
| 喂食 | 提高饱食和少量成长值 | 吃东西、开心 |
| 抚摸 | 提高心情和亲密度 | 眯眼、蹭动、弹跳 |
| 玩耍 | 提高心情并消耗少量体力 | 扑动、拍爪 |
| 休息 | 恢复体力 | 趴下、睡觉 |

要求：

- 操作提交给云服务器确认。
- 在线的另一台电脑在合理时间内看到状态变化。
- 对方离线时，在下次启动或恢复连接后同步。
- 每个操作带唯一事件编号，重复提交不会重复生效。
- 同一种操作有短冷却和每日成长值上限，避免重复点击刷成长。

### 6.3 宠物传话

- 用户可输入不超过 100 个字符的纯文字消息。
- 对方上线或恢复连接后，由宠物通过送信动作和气泡展示。
- 每条消息只自动弹出一次。
- 未发送的消息明确显示“等待发送”。
- 用户可以在最近互动中再次查看。
- 第一版不支持图片、文件、语音、撤回和完整聊天会话。

### 6.4 互动记录

- 展示最近的照顾、礼物、传话和成长解锁事件。
- 默认最多显示最近 50 条。
- 不记录状态自然衰减等低价值事件。
- 记录应包含行为人、事件类型和时间。
- 两位用户之外的任何人都不能访问记录。

### 6.5 轻量礼物

- 第一版内置约 6～10 种固定小道具。
- 礼物属于宠物，双方都能看到。
- 收到礼物时触发气泡、图标或临时程序动画。
- 礼物事件写入互动记录。
- 第一版不做货币、充值、商城和复杂背包。

### 6.6 共同成长

- 任意一方照顾宠物都可获得少量共同成长值。
- 24 小时内双方都参与时，提供一次共同陪伴奖励。
- 成长值和成长阶段不会下降或清零。
- 必要功能不能被成长等级锁定。
- 成长里程碑只解锁额外表情与动作。
- 默认不展示精确经验值，只显示模糊成长阶段。

建议的四个阶段：

| 阶段 | 内容 |
| --- | --- |
| 初次相识 | 基础待机、照顾、睡眠和送信 |
| 渐渐熟悉 | 新开心表情、挥手等额外反馈 |
| 默契伙伴 | 庆祝、特殊睡姿或玩耍动作 |
| 共同回忆 | 双方参与后可能触发的稀有动作 |

## 7. 状态系统

客户端内部可使用 0～100 的数值计算，但默认不向用户显示精确数值。

| 内部状态 | 面板文案 | 常见表现 |
| --- | --- | --- |
| 80～100 | 精神很好 | 活跃、开心 |
| 50～79 | 状态不错 | 正常待机 |
| 25～49 | 有一点累或饿 | 动作放慢、打哈欠 |
| 0～24 | 想休息或吃点东西 | 更多睡眠或提示动作 |

规则：

- 状态缓慢变化。
- 低状态只改变表现，不造成永久损失。
- 一到两次简单互动即可明显恢复。
- 不出现责怪某一位用户的文案。
- 长时间无人照顾时，宠物可以睡觉、自己玩耍或外出，而不是遭受严重负面后果。
- 通知有冷却时间，并受本机免打扰设置控制。

## 8. 角色表现

### 8.1 性格表现

- 活泼、调皮、反应明显。
- 待机时可甩尾、探头、拍爪和眨眼。
- 点击时可躲开、反拍或弹跳。
- 收到礼物时围绕礼物观察或转圈。
- 送信时叼着纸条出现，等待用户点击。
- 双方共同照顾后可触发庆祝动作。
- 气泡文案短而活泼，但不打断工作或责怪用户。

### 8.2 原型阶段动画

普通透明 PNG 通过程序实现：

- 呼吸缩放。
- 上下浮动。
- 左右摇摆。
- 点击弹跳。
- 睡眠缩放或轻微起伏。
- 送信和礼物使用临时图标与气泡。

### 8.3 正式精灵图

后续至少补充：

- 待机与眨眼。
- 左右行走。
- 抚摸与开心。
- 睡觉与醒来。
- 吃东西和玩耍。
- 饥饿与疲倦。
- 送信。
- 成长解锁的额外表情与动作。

动画业务层只引用 `idle`、`happy`、`sleep`、`feed`、`play`、`message` 等状态名，避免养成逻辑直接依赖具体图片。

## 9. 同步与离线要求

- 服务器保存唯一可信的共享状态。
- 客户端本地保存最近一次状态快照和待发送队列。
- 断网时桌宠仍可显示和接受操作。
- 网络恢复后按原始时间顺序补发。
- 每个事件具有全局唯一编号。
- 两人同时操作时都保留为事件，不使用整份状态覆盖。
- 客户端显示在线、同步中、离线和发送失败状态。
- 留言和礼物在服务器确认前不能显示为已送达。
- 服务器短暂故障不能导致状态清空或客户端无法启动。

## 10. 建议架构边界

### 10.1 复用 OpenPets

- Electron 桌面应用框架。
- 透明宠物窗口。
- 拖动、置顶、鼠标穿透和多显示器处理。
- 托盘与基础设置。
- 宠物包和精灵图渲染方式。
- 插件运行框架及 Virtual Pet 插件的交互思路。

### 10.2 独立实现

- 两位用户身份和邀请码。
- 云端宠物房间。
- 服务端权威状态。
- 异步事件和传话。
- 离线队列与幂等处理。
- 礼物和共同成长规则。
- 后续实时在线状态和 WebSocket 互动。

### 10.3 暂不实现或默认隐藏

- AI 编程助手和 MCP 集成。
- 公共宠物目录。
- 第三方插件商店。
- 跨平台支持。
- 多宠物同时运行。
- 公开账号体系。

## 11. 非功能需求

- 两个绑定设备之外的客户端不能读取或修改共享数据。
- 客户端与服务器通信必须使用加密连接。
- 设备令牌可撤销，不能把长期凭据硬编码在程序中。
- 服务端定期备份共享状态和未过期事件。
- 客户端常驻时应保持合理的 CPU 和内存占用。
- 桌宠不能长期遮挡关键窗口区域。
- 用户可随时静音、隐藏桌宠或启用免打扰。
- 应记录必要的本地诊断日志，但不能记录传话正文和设备令牌。

## 12. MVP 验收标准

1. 两台 Windows 电脑可使用不同邀请码绑定同一个宠物房间。
2. 双方看到相同的宠物名称、成长阶段和共享状态。
3. A 的喂食、抚摸、玩耍或休息操作能同步到 B。
4. B 离线时，A 的操作在 B 下次上线后正确出现。
5. 同一事件重复发送不会重复增加状态或成长值。
6. 一方可以发送短消息，另一方由宠物气泡接收。
7. 未成功上传的消息明确标记为等待发送。
8. 礼物可送出、展示并写入互动记录。
9. 双方在规定时间内都参与后，共同成长规则正确生效。
10. 达到成长里程碑后，两台客户端同时解锁相同表现。
11. 断网期间桌宠仍能运行，联网后自动完成同步。
12. 用户可以拖动、隐藏、静音和调整桌宠活动频率。

## 13. 开发阶段

### 阶段 A：技术验证

- 运行并打包 OpenPets Windows 客户端。
- 加载普通透明 PNG 宠物。
- 创建最小 Shared Pet 插件。
- 插件向测试服务器提交一次抚摸事件。
- 第二台客户端接收事件并显示气泡。
- 验证断网队列和恢复补发。

### 阶段 B：单机桌宠原型

- 完成 PNG 程序动画。
- 完成拖动、点击、托盘和本机设置。
- 建立动画状态接口。
- 实现本机状态面板和轻松陪伴规则。

### 阶段 C：双人异步 MVP

- 完成邀请码、设备绑定和宠物房间。
- 完成服务端事件、共享状态与数据持久化。
- 完成共同照顾、传话和互动记录。
- 完成轻量礼物与共同成长。
- 完成离线队列、重试和幂等处理。

### 阶段 D：稳定性与交付

- 完成权限检查、令牌撤销和备份。
- 测试服务器故障、重复事件和两端同时操作。
- 优化常驻性能和桌面打扰程度。
- 构建两台电脑可安装的 Windows 包。

### 后续阶段

- 正式精灵图。
- 实时在线状态与动作。
- 双人实时小游戏。
- 更丰富的成长动作。

## 14. 第一项技术里程碑

在全面开发前，应先完成一个端到端验证：

> 用户 A 在桌宠上执行“抚摸” → 云服务器记录事件 → 用户 B 的桌宠收到更新并显示气泡；B 离线时则在下次上线后完成同步。

这个闭环通过后，再扩展其余养成和互动功能。

## 15. 参考资料

- [OpenPets 仓库](https://github.com/alvinunreal/openpets)
- [OpenPets 宠物模型、安装与渲染文档](https://github.com/alvinunreal/openpets/blob/main/docs/pets.md)
- [OpenPets Virtual Pet 插件](https://github.com/alvinunreal/openpets/tree/main/plugins/official/openpets.virtual-pet)
- [OpenPets MIT 许可证](https://github.com/alvinunreal/openpets/blob/main/LICENSE)

## 16. 当前执行状态与近期重点

### 16.1 已完成

- 项目整体以 OpenPets 桌面端、宠物包和插件 SDK 为底座，没有另建桌面壳。
- 已建立共享宠物服务端、两人邀请码绑定、幂等事件、离线队列和共同参与奖励的最小闭环。
- 已建立 `openpets.shared-pet` 插件，并通过真实 OpenPets SDK v3 测试桩验证。
- 已制作团团的 9 组、每组 8 帧正式精灵图，覆盖待机、左右移动、互动、跳跃、情绪、休息、工作和查看等状态。
- 已修复点击宠物时因主进程日志管道断开产生的 `EPIPE` 弹窗问题。
- 已修复动画串帧、碎尾巴、越界和身体锚点漂移：构建时仅保留每帧主主体，统一动作行缩放，并自动检查边界、碎片和锚点误差。
- 开发插件已改为由 OpenPets 通过 `OPENPETS_DEV_PLUGIN_PATHS` 直接加载真实源目录，消除整目录复制导致的多轮热重载和 `EPERM` 警告。
- 已提供 `npm run dev:openpets` 一键准备并启动命令，标准输出与错误日志按启动会话保存在 `data/`。
- 未配对时点击猫猫现在只提供本地动作和低频配置提示，不再污染离线队列；传话、礼物和照顾命令在绑定前会给出明确指引。
- 双客户端 HTTP 场景已覆盖邀请码一次性使用、离线恢复、事件幂等、消息游标、双方共同奖励、状态收敛和设备令牌撤销。
- 离线期间积累的多条传话和礼物会在恢复后合并展示，只有展示成功才推进事件游标，不再只显示最后一条并跳过其余内容。

### 16.2 动画质量验收基线

- 精灵图固定为 8 列 × 9 行，每帧 `192 × 208`。
- 非跳跃动作的可见底边保持一致；跳跃动作允许按动作轨迹离地。
- 每帧四周至少保留 2px 透明安全区，不允许内容触碰或越过帧边界。
- 同一动作行使用统一缩放比例，不因单帧姿态改变而忽大忽小。
- 身体横向锚点误差不超过 1px；尾巴、纸张等附属部分不能带动主体瞬移。
- 不允许相邻动作行或相邻帧的尾巴、脚、阴影等碎片串入当前帧。

### 16.3 下一批优先事项

1. 完成两台 Windows 电脑的真实配对与断网恢复联调，记录端到端延迟和失败表现。
2. 将喂食、抚摸、玩耍、休息映射到更准确的专属动作，而不是复用通用反馈。
3. 补充面向普通用户的首次配置和服务器地址设置流程。
4. 在不打扰工作的前提下增加低频自主行为、动作冷却和免打扰联动。
5. 建立可重复的 Windows 安装包构建与双机发布流程。
